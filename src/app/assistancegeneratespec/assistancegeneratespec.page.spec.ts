import { ComponentFixture, TestBed } from '@angular/core/testing';
import { AssistancegeneratespecPage } from './assistancegeneratespec.page';

describe('AssistancegeneratespecPage', () => {
  let component: AssistancegeneratespecPage;
  let fixture: ComponentFixture<AssistancegeneratespecPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(AssistancegeneratespecPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
