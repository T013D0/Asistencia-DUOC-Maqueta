import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Assistanceregistry1Page } from './assistanceregistry1.page';

describe('Assistanceregistry1Page', () => {
  let component: Assistanceregistry1Page;
  let fixture: ComponentFixture<Assistanceregistry1Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Assistanceregistry1Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
