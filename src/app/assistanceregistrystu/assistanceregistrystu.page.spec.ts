import { ComponentFixture, TestBed } from '@angular/core/testing';
import { AssistanceregistrystuPage } from './assistanceregistrystu.page';

describe('AssistanceregistrystuPage', () => {
  let component: AssistanceregistrystuPage;
  let fixture: ComponentFixture<AssistanceregistrystuPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(AssistanceregistrystuPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
